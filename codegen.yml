schema: ./utils/graphql/schema.graphql
documents: ./utils/graphql/document.graphql
generates:
  ./utils/generated.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
config:
  typesPrefix: I
  avoidOptionals: true
  withHooks: true
  withHOC: false
  withComponent: false
  withResultType: true
hooks:
  afterAllFileWrite:
    - prettier --write
