import * as React from "react";
import FeatureContent from "../markdown/features";

export default function FeatureList() {
  return (
    <section className="FeaturesContainer">
      <FeatureContent />
    </section>
  );
}
