{
  "rules": [
    {
      "pathname": "/api",
      "method": ["GET", "POST", "PUT", "DELETE", "OPTIONS"],
      "dest": "http://localhost:4000"
    },
    { "pathname": "/**", "dest": "http://localhost:3000" }
  ]
}
