/// <reference types="@types/jest-environment-puppeteer" />

import { jest, it, describe } from "@jest/globals";

jest.setTimeout(50000);

describe("Downwrite E2E: Preview Image", () => {
  it.todo("should be able to toggle visibility of post");
  it.todo("should be able to render markdown of post and author");

  it.todo("should show error of private post");
});
