/// <reference types="@types/jest-environment-puppeteer" />

import { jest, it, describe } from "@jest/globals";
// import { getByTestId } from "./utils";

// import { user } from "../../fixtures/user.json";

jest.setTimeout(50000);

describe("Downwrite E2E: Settings", () => {
  it.todo("should update username and reflect that in the UI");
  it.todo(
    "should update password and be able to log in with different username and password"
  );

  it.todo("should detail user stats");
});
